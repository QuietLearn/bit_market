<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.stylefeng.guns.modular.huobi.dao.KlineMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.stylefeng.guns.modular.huobi.model.Kline">
        <id column="id" property="id" />
        <result column="amount" property="amount" />
        <result column="count" property="count" />
        <result column="open" property="open" />
        <result column="close" property="close" />
        <result column="low" property="low" />
        <result column="high" property="high" />
        <result column="vol" property="vol" />
        <result column="symbol" property="symbol" />
        <result column="peroid" property="peroid" />

        <result column="gmt_created" property="gmtCreated" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, amount, count, open, close, low, high, vol,symbol,peroid,gmt_created
    </sql>

    <select id="getAllIds" resultType="integer" parameterType="map">
        select id
        from kline
        where peroid = #{period}
        order by id desc
        limit 0,#{size}
    </select>

    <select id="selectInserted" resultMap="BaseResultMap" parameterType="map">
        select <include refid="Base_Column_List"/>
        from kline
        where peroid = #{period}
        order by id desc
        limit 0,#{size}
    </select>
</mapper>
